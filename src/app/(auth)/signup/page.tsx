import { AuthCard } from "@/components/auth-card";

export const metadata = { title: "Create account — ExpenseTracking Audit" };

export default function SignupPage() {
  return <AuthCard mode="signup" />;
}
